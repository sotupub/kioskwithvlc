<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Selected state -->
    <item android:state_selected="true" android:drawable="@color/custom_purple" />
    <!-- Pressed state -->
    <item android:state_pressed="true" android:drawable="@color/custom_purple" />
    <!-- Normal state -->
    <item android:drawable="@color/light_gray" />
</selector>
